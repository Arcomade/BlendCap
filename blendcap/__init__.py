# BlendCap feature package. The root __init__.py (the extension entry
# point) owns the AddonPreferences class and delegates register/unregister
# to registration.py here. Submodules hold the operators, panels,
# properties, helpers, and the retarget engine.
